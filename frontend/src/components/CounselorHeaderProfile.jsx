/**
 * Deprecated in favor of CounselorTopNav profile dropdown.
 * Kept to avoid touching every existing page import immediately.
 */
export default function CounselorHeaderProfile() {
  return null;
}
